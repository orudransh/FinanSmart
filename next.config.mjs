 /** @type {import('next').NextConfig} */
 const nextConfig = {
    images:{
        unoptimized:true
    }
}; 

export default nextConfig;


/*/** @type {import('next').NextConfig} */
/*const nextConfig = {
  reactStrictMode: true, // Helps detect problems during development
  experimental: {
      appDir: true, // Ensure compatibility with your Next.js version
  },
  images: {
      unoptimized: true, // Useful for static export
  },
};

export default nextConfig;
*/
